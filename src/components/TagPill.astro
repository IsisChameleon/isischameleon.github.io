---
const { label, active = false } = Astro.props;
---
<span
  class={`inline-flex items-center rounded-full border px-3 py-1 text-xs font-medium transition-colors ${
    active
      ? 'border-accent/60 bg-accent/10 text-accent'
      : 'border-base-200 dark:border-base-800 text-base-600 dark:text-base-300'
  }`}
>
  {label}
</span>
