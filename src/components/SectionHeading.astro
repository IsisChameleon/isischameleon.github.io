---
const { title, subtitle } = Astro.props;
---
<div class="flex flex-col gap-3">
  <h2 class="text-2xl md:text-3xl font-semibold tracking-tight text-base-950 dark:text-base-50">{title}</h2>
  {subtitle ? <p class="text-base-600 dark:text-base-300 max-w-2xl">{subtitle}</p> : null}
</div>
